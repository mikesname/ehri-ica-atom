Index: lib/vendor/symfony/lib/config/sfViewConfigHandler.class.php
===================================================================
--- lib/vendor/symfony/lib/config/sfViewConfigHandler.class.php	(revision 10271)
+++ lib/vendor/symfony/lib/config/sfViewConfigHandler.class.php	(working copy)
@@ -246,13 +246,13 @@
     // Merge the current view's stylesheets with the app's default stylesheets
     $stylesheets = $this->mergeConfigValue('stylesheets', $viewName);
     $tmp = array();
-    foreach ((array) $stylesheets as $css)
+    foreach ((array) $stylesheets as $key => $stylesheet)
     {
       $position = '';
-      if (is_array($css))
+      if (is_array($stylesheet))
       {
-        $key = key($css);
-        $options = $css[$key];
+        $options = $stylesheet;
+        $stylesheet = $key;
         if (isset($options['position']))
         {
           $position = $options['position'];
@@ -261,21 +261,20 @@
       }
       else
       {
-        $key = $css;
         $options = array();
       }
 
-      if ('-*' == $key)
+      if ('-*' == $stylesheet)
       {
         $tmp = array();
       }
-      else if ('-' == $key[0])
+      else if ('-' == $stylesheet[0])
       {
-        unset($tmp[substr($key, 1)]);
+        unset($tmp[substr($stylesheet, 1)]);
       }
       else
       {
-        $tmp[$key] = sprintf("  \$response->addStylesheet('%s', '%s', %s);", $key, $position, str_replace("\n", '', var_export($options, true)));
+        $tmp[$stylesheet] = sprintf("  \$response->addStylesheet('%s', '%s', %s);", $stylesheet, $position, str_replace("\n", '', var_export($options, true)));
       }
     }
 
